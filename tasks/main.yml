---
# Main task: Load package installers, or not, based on tags.

- name: Dob dev » Load tasks dispatcher for ‘zoidy_dob-dev’
  include_role:
    name: zoidy_dob-dev
    tasks_from: all
  tags:
    - dev-dob

